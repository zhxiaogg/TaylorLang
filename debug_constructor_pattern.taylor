// Simple constructor pattern test for debugging
type Result<T, E> = Ok(T) | Error(E)

val result = Ok(42)
val message = match result {
    case Ok(value) => "Success"
    case Error(err) => "Error"  
}
println(message)