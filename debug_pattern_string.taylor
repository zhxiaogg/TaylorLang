// Pattern test with string concatenation
type Result<T, E> = Ok(T) | Error(E)

val result = Ok(42)
val message = match result {
    case Ok(x) => "Value: " + x
    case Error(e) => "Error occurred"
}

println(message)