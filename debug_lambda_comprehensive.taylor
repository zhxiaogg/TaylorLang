// Test individual lambda patterns to find issues

// This works
val simple = x => x

// This works  
val arithmetic = x => x * 2

// Test comparison
val comparison = x => x >= 18

// Test conditional
val conditional = x => if (x > 0) x else -x

// Test multi-parameter
val multiParam = (x, y) => x + y