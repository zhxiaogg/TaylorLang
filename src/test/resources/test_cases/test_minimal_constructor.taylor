// Minimal constructor pattern test
type Result<T, E> = Ok(T) | Error(E)

val result = Ok(42)
val output = match result {
    case Ok(value) => value
    case Error(err) => 0
}
println("Output: " + output)
println("Test completed!")